import work.DeserializeUnparser;
import work.SerializeWork1;
import work.Unparser;
import work.Work1;

public class SerializeMain {
    public static void main(String[] args) {
        SerializeWork1.create();
        DeserializeUnparser.unpars();
    }
}
